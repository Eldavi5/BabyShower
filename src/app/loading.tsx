export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-sky-50 text-slate-700">
      <p className="animate-pulse text-lg font-semibold">Cargando invitación...</p>
    </main>
  );
}
